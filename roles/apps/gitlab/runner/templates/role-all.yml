apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gitlab-runner
  labels:
    k8s-app: gitlab-runner

aggregationRule:
  clusterRoleSelectors:
    - matchLabels:
        rbac.{{ cluster_domain }}/gitlab-runner-role: "true"
rules: []

